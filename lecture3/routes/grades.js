module.exports = function (app) {
  app.data = app.data || {};
  app.data.grades = {};

  // TODO:: Set up the following routes:

  // app.get('/grades', fn);
  // app.get('/grades/:name', fn2);
  // app.post('/grade', fn3);
};
